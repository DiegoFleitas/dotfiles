# Chezmoi configuration (generated from .chezmoi.toml.tmpl in source).
# Run scripts execute in alphabetical order:
#   1. run_once_before_install.sh  - system deps, brew, nvm, oh-my-zsh, pyenv (once)
#   2. run_once_z_after_install.sh - default shell, brew bundle, pyenv install (once)
# apps.sh is not a runscript; run it manually after apply if needed.

[data]
# Optional: set git.name and git.email here or they will be prompted on first apply.
# git = { name = "Your Name", email = "you@example.com" }
