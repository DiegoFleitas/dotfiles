# tap "homebrew/cask"
# Node: nvm + ~/.nvmrc; yarn/pnpm via Corepack in install/before_finalize.sh (not Brew)
brew "biome"   # lint + format for JS/TS (replaces eslint + prettier in one binary)
brew "wget"
brew "gcc" # Recommended by Homebrew
# brew "node" # use nvm (avoid brew node shadowing default Node from nvm)
brew "awscli"
# brew "ffmpeg"
# Communication tools
# ... tools like slack, zoom, teams ...
# cask "google-chrome"
# cask "visual-studio-code"
brew "jq"
